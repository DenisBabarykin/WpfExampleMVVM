<Window x:Class="FactorialListApp.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainView" Height="350" Width="525">
    <Grid>
        <ComboBox Height="30"
                  VerticalAlignment="Top"
                  ItemsSource="{Binding Factorials}" >
        </ComboBox>
        <Button Width="200"
                Height="30"
                VerticalAlignment="Bottom"
                HorizontalAlignment="Right"
                Margin="10"
                Foreground="White"
                Background="#017ACD"
                Content="{Binding BtnText}"
                FontSize="18"
                Click="Button_Click"
                />
    </Grid>
</Window>
